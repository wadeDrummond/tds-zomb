///playerCollisionCheck(x, y, obj)

var _tempCheck = collision_line_point(x, y, argument0, argument1, argument2, true, true);
var ID = _tempCheck[0];

var Check1 = place_meeting(argument0, argument1, argument2);
//var Check2 = ID != noone;
var Check2 = false;

if Check2 clAddMessage(string(ID));

return (Check1 or Check2);
